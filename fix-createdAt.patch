@'
diff --git a/src/pages/partners/index.astro b/src/pages/partners/index.astro
index 1111111..2222222 100644
--- a/src/pages/partners/index.astro
+++ b/src/pages/partners/index.astro
@@ -1,5 +1,18 @@
 ---
+// NOTE:
+// В архивных данных поле createdAt может отсутствовать в типе ArchiveItem.
+// Чтобы сборка не падала, расширяем тип локально и работаем через optional.
+
+type ArchiveItemEx = ArchiveItem & {
+  createdAt?: string;
+};
+
+const archiveItemsEx = (archiveItems as ArchiveItemEx[]);
+
 const items = archiveItems
-  .sort((a, b) => (b.createdAt || '').localeCompare(a.createdAt || ''));
+  // сортируем по createdAt если есть, иначе просто стабильная сортировка
+  .sort((a, b) => ((b as ArchiveItemEx).createdAt ?? '').localeCompare(((a as ArchiveItemEx).createdAt ?? '')));
 ---
@@ -148,7 +161,7 @@
                 <div class="text-xs text-muted-foreground">
-                  {item.createdAt ? <span>{item.createdAt}</span> : null}
+                  {(item as ArchiveItemEx).createdAt ? <span>{(item as ArchiveItemEx).createdAt}</span> : null}
                 </div>
               </div>
             </a>
'@ | Set-Content -Encoding UTF8 .\fix-createdAt.patch
