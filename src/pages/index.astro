---
import BaseLayout from '@/layouts/BaseLayout.astro';
---

<BaseLayout
  title="XTIR — Точная техника. Жёсткая надёжность."
  description="XTIR — разработка и производство электронно-механического оборудования для стрельбы."
>

<section class="hp-hero">
  <div class="hp-bg">
    <div class="hp-glow-l"></div>
    <div class="hp-glow-r"></div>
    <div class="hp-grid"></div>
    <div class="hp-scan"></div>
  </div>

  <div class="hp-tricolor">
    <span class="tc1"></span><span class="tc2"></span><span class="tc3"></span>
  </div>

  <div class="hp-c hp-c-tl"></div>
  <div class="hp-c hp-c-tr"></div>
  <div class="hp-c hp-c-bl"></div>
  <div class="hp-c hp-c-br"></div>

  <!-- Пуля — медленная трассирующая -->
  <div class="hp-bullet-wrap">
    <div class="hp-bullet"></div>
    <div class="hp-bullet hp-bullet--2"></div>
  </div>

  <div class="hp-body">
    <div class="hp-center">
      <div class="hp-badge">
        <span class="hp-bdot"></span>
        XTIR · Электронно-механические решения для стрельбы
      </div>
      <div class="hp-logo-box">
        <img src="/images/logo.png" alt="XTIR" class="hp-logo"/>
      </div>
      <div class="hp-slogan">
        <div class="hp-sbar"><span class="tc1"></span><span class="tc2"></span><span class="tc3"></span></div>
        <div class="hp-stext">Инженерные решения для стрелковых тренировок</div>
      </div>
      <div class="hp-desc">
        Проектирование и производство систем под реальные условия эксплуатации.<br/>
        Точность. Надёжность. Ресурс.
      </div>
      <div class="hp-acts">
        <a href="/contact/" class="hp-btn1">Обсудить проект</a>
        <a href="/products/" class="hp-btn2">Смотреть каталог →</a>
      </div>
    </div>
  </div>

  <div class="hp-stats">
    <div class="hp-stat"><div class="hp-sn">20+</div><div class="hp-sl">лет в отрасли</div></div>
    <div class="hp-sdiv"></div>
    <div class="hp-stat"><div class="hp-sn">30+</div><div class="hp-sl">позиций каталога</div></div>
    <div class="hp-sdiv"></div>
    <div class="hp-stat"><div class="hp-sn">РФ</div><div class="hp-sl">производство</div></div>
    <div class="hp-sdiv"></div>
    <div class="hp-stat"><div class="hp-sn">24/7</div><div class="hp-sl">поддержка</div></div>
  </div>
</section>

<!-- НАПРАВЛЕНИЯ -->
<section class="hp-sec hp-dark">
  <div class="hp-wrap">
    <div class="hp-hd">
      <div class="hp-kicker">Направления</div>
      <div class="hp-title">Оборудование под любую задачу</div>
      <div class="hp-sub">Три класса решений — от электронных мишеней до полигонных комплексов.</div>
    </div>
    <div class="hp-cards">
      <a href="/products/" class="hp-card">
        <div class="hp-cimg">
          <img src="/products-img/pmu-100/pmu100-1.png" alt="Электронные мишени" loading="lazy"/>
          <div class="hp-cimgo"></div>
          <div class="hp-cnum">01</div>
        </div>
        <div class="hp-cbody">
          <div class="hp-ct">Электронные мишени</div>
          <div class="hp-cd">Повторяемость, контроль попаданий, статистика. Для спорта, силовых структур и тиров.</div>
          <div class="hp-ctags"><span>Тренировка</span><span>Обратная связь</span></div>
          <div class="hp-cmore">Смотреть продукты →</div>
        </div>
      </a>
      <a href="/products/" class="hp-card hp-card-warm">
        <div class="hp-cimg">
          <img src="/products-img/kaban/1.png" alt="Движущиеся цели" loading="lazy"/>
          <div class="hp-cimgo"></div>
          <div class="hp-cnum">02</div>
        </div>
        <div class="hp-cbody">
          <div class="hp-ct">Движущиеся цели</div>
          <div class="hp-cd">Моделирование реальных ситуаций: скорость, тайминг, реакция. Динамические сценарии.</div>
          <div class="hp-ctags"><span>Динамика</span><span>Сценарии</span></div>
          <div class="hp-cmore hp-cmore-warm">Как это устроено →</div>
        </div>
      </a>
      <a href="/products/" class="hp-card">
        <div class="hp-cimg">
          <img src="/products-img/pak/1 (1).png" alt="Управление" loading="lazy"/>
          <div class="hp-cimgo"></div>
          <div class="hp-cnum">03</div>
        </div>
        <div class="hp-cbody">
          <div class="hp-ct">Управление и интеграции</div>
          <div class="hp-cd">Много узлов на объекте — предсказуемая работа: статусы, телеметрия, журнал событий.</div>
          <div class="hp-ctags"><span>Связь</span><span>Надёжность</span></div>
          <div class="hp-cmore">Технологии →</div>
        </div>
      </a>
    </div>
  </div>
</section>

<!-- ПОЧЕМУ XTIR -->
<section class="hp-sec hp-darker">
  <div class="hp-wrap hp-why">
    <div class="hp-wl">
      <div class="hp-kicker">Почему XTIR</div>
      <div class="hp-title">Инженерный подход.<br/>Предсказуемый результат.</div>
      <div class="hp-sub">20 лет в отрасли. Оборудование которое реально работает — не на презентации, а на объекте.</div>
      <a href="/about/" class="hp-lnk">О компании →</a>
    </div>
    <div class="hp-wr">
      <div class="hp-wc"><div class="hp-wi">⚙️</div><div class="hp-wt">Проектирование</div><div class="hp-wd">Требования, расчёт, прототипирование, документация. Без догадок.</div></div>
      <div class="hp-wc"><div class="hp-wi">🏭</div><div class="hp-wt">Производство</div><div class="hp-wd">Сборка, контроль, повторяемость. Узлы рассчитаны на реальную эксплуатацию.</div></div>
      <div class="hp-wc"><div class="hp-wi">🔗</div><div class="hp-wt">Интеграции</div><div class="hp-wd">Связь, управление, сценарии. Подстраиваемся под объект и задачи заказчика.</div></div>
      <div class="hp-wc"><div class="hp-wi">🛠</div><div class="hp-wt">Поддержка</div><div class="hp-wd">Сопровождение, диагностика, обновления. Без выходных.</div></div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="hp-sec hp-dark" style="position:relative;overflow:hidden;">
  <div class="hp-ctabg"></div>
  <div class="hp-wrap hp-cta">
    <div class="hp-ctl">
      <div class="hp-title">Готовы начать?</div>
      <div class="hp-sub">Опишите задачу — ответим быстро и по делу.</div>
      <div class="hp-acts" style="margin-top:28px;">
        <a href="/contact/" class="hp-btn1">Обсудить проект</a>
        <a href="/products/" class="hp-btn2">Каталог →</a>
      </div>
    </div>
    <div class="hp-ctr">
      <a href="tel:+79154250095" class="hp-cnt"><span class="hp-cl">Телефон</span><span class="hp-cv">+7 (915) 425-00-95</span></a>
      <a href="tel:+79162962469" class="hp-cnt"><span class="hp-cl">&nbsp;</span><span class="hp-cv">+7 (916) 296-24-69</span></a>
      <a href="mailto:info@xtir.ru" class="hp-cnt"><span class="hp-cl">Email</span><span class="hp-cv">info@xtir.ru</span></a>
    </div>
  </div>
</section>

</BaseLayout>

<style is:global>
  ::-webkit-scrollbar { width: 3px !important; }
  ::-webkit-scrollbar-track { background: #060a0e !important; }
  ::-webkit-scrollbar-thumb {
    background: linear-gradient(to bottom,
      #ffffff 0%, #ffffff 33%,
      #0039a6 33%, #0039a6 66%,
      #d52b1e 66%, #d52b1e 100%
    ) !important;
    border-radius: 0 !important;
  }
  @supports (scrollbar-color: auto) {
    * { scrollbar-width: thin; scrollbar-color: #0039a6 #060a0e; }
  }
  /* XTIR_REMOVE_BULLETS */
  /* Р“Р»СѓС€РёРј РґРµРєРѕСЂР°С‚РёРІРЅС‹Рµ "РїСѓР»Рё/С‚РѕС‡РєРё/РїСѓР»СЊСЃС‹", РЅРµ Р»РѕРјР°СЏ СЃС‚СЂСѓРєС‚СѓСЂСѓ */
  .bullet, .bullets, .pulse, .pulses, .dot, .dots, .marker, .markers,
  .hero__bdot, .cta__dot, .cta__bdot, .ui-dot, .ui-bullet{
    display: none !important;
  }

  /* Р•СЃР»Рё РїСѓР»Рё РЅР°СЂРёСЃРѕРІР°РЅС‹ РїСЃРµРІРґРѕСЌР»РµРјРµРЅС‚Р°РјРё */
  .bullet::before, .bullet::after,
  .pulse::before, .pulse::after,
  .dot::before, .dot::after,
  .hero__badge::before, .hero__badge::after{
    content: none !important;
    display: none !important;
  }

  /* РќР° РІСЃСЏРєРёР№ СЃР»СѓС‡Р°Р№: СЃРїРёСЃРєРё РЅР° РіР»Р°РІРЅРѕР№ Р±РµР· РјР°СЂРєРµСЂРѕРІ */
  .sec ul, .hero ul{
    list-style: none;
    padding-left: 0;
  }
  .sec li::marker, .hero li::marker{ content: ""; }
/* XTIR_NO_BULLETS */
.hero [class*="bullet"], .hero [class*="streak"], .hero [class*="tracer"], .hero [class*="shot"], .hero [class*="sweep"]{
  animation: none !important;
  transition: none !important;
  opacity: 0 !important;
  display: none !important;
}
.hero :where(.hero__bg, .hero__grid, .hero__scan)::before,
.hero :where(.hero__bg, .hero__grid, .hero__scan)::after{
  animation: none !important;
}
/* XTIR_NO_BULLETS_MAX */
.hero :where(.hero__bg,.hero__grid,.hero__scan,.hero__fx,.hero__decor,.hero__effects,.hero__backdrop,.bg,.grid,.scan,.fx,.decor,.effects,.backdrop){
  animation: none !important;
  transition: none !important;
  filter: none !important;
}
.hero :where(.hero__bg,.hero__grid,.hero__scan,.hero__fx,.hero__decor,.hero__effects,.hero__backdrop,.bg,.grid,.scan,.fx,.decor,.effects,.backdrop) *,
.hero :where(.hero__bg,.hero__grid,.hero__scan,.hero__fx,.hero__decor,.hero__effects,.hero__backdrop,.bg,.grid,.scan,.fx,.decor,.effects,.backdrop)::before,
.hero :where(.hero__bg,.hero__grid,.hero__scan,.hero__fx,.hero__decor,.hero__effects,.hero__backdrop,.bg,.grid,.scan,.fx,.decor,.effects,.backdrop)::after{
  animation: none !important;
  transition: none !important;
  animation-play-state: paused !important;
}
/* Р•СЃР»Рё вЂњРїСѓР»РёвЂќ СЃРґРµР»Р°РЅС‹ РєР°Рє С‚РѕРЅРєРёРµ Р°Р±СЃРѕР»СЋС‚РЅС‹Рµ Р»РёРЅРёРё */
.hero :where(span, i, b, em, strong, div)[class]{
  /* С‚РѕР»СЊРєРѕ РґР»СЏ СЏРІРЅРѕ РґРµРєРѕСЂР°С‚РёРІРЅС‹С… СЌР»РµРјРµРЅС‚РѕРІ РїРѕ РёРјРµРЅР°Рј */
}
.hero [class*="bullet" i],
.hero [class*="tracer" i],
.hero [class*="streak" i],
.hero [class*="shot" i],
.hero [class*="beam" i],
.hero [class*="laser" i],
.hero [class*="ray" i],
.hero [class*="sweep" i],
.hero [class*="scan" i],
.hero [class*="line" i]{
  display: none !important;
  opacity: 0 !important;
  animation: none !important;
  transition: none !important;
}
/* Р§Р°СЃС‚Рѕ вЂњРїРѕР»РѕСЃРєРёвЂќ Р¶РёРІСѓС‚ РІ РїСЃРµРІРґРѕСЌР»РµРјРµРЅС‚Р°С… hero */
.hero::before, .hero::after{
  animation: none !important;
  transition: none !important;
}
/* XTIR_READABILITY_LIGHT */
:root{
  --xtir-midlift: rgba(255,255,255,.028);
  --xtir-midlift2: rgba(255,255,255,.018);
  --xtir-text-strong: rgba(255,255,255,.92);
  --xtir-text: rgba(255,255,255,.78);
  --xtir-text-soft: rgba(255,255,255,.66);
  --xtir-line: rgba(255,255,255,.12);
  --xtir-line2: rgba(255,255,255,.08);
  --xtir-glowA: rgba(120,170,255,.12);
  --xtir-glowB: rgba(190,120,255,.08);
}

/* РћР±С‰Р°СЏ С‡РёС‚Р°Р±РµР»СЊРЅРѕСЃС‚СЊ */
.hero, .sec{ color: var(--xtir-text); }
.h1, .h2, .hero__slogan-text{ color: var(--xtir-text-strong); }
.sub, .hero__sub{ color: var(--xtir-text-soft); }

/* РњСЏРіС‡Рµ вЂњС€РІС‹вЂќ РјРµР¶РґСѓ Р±Р»РѕРєР°РјРё */
.sec{
  position: relative;
  border-top: 1px solid var(--xtir-line2);
}
.sec:first-of-type{ border-top: none; }

/* РџРѕРґРЅСЏС‚СЊ СЃСЂРµРґРЅРёРµ С‚РѕРЅР° СЃРµРєС†РёР№, С‡С‚РѕР±С‹ РЅРµ Р±С‹Р»Рѕ вЂњСЃР»РёС€РєРѕРј С‚РµРјРЅРѕвЂќ */
.sec--dark, .sec--darker{
  background:
    radial-gradient(1200px 700px at 50% 0%, var(--xtir-midlift), transparent 62%),
    radial-gradient(900px 540px at 20% 10%, var(--xtir-glowA), transparent 68%),
    radial-gradient(900px 540px at 80% 20%, var(--xtir-glowB), transparent 72%),
    linear-gradient(180deg, rgba(255,255,255,.04), rgba(0,0,0,.00) 58%);
}

/* Р›С‘РіРєР°СЏ РїРѕРґСЃРІРµС‚РєР° Р·РѕРЅС‹ Р·Р°РіРѕР»РѕРІРєР° СЃРµРєС†РёРё (РµСЃР»Рё РµСЃС‚СЊ .sec__hd) */
.sec__hd{
  position: relative;
}
.sec__hd::after{
  content:"";
  position:absolute;
  left:-6px; right:-6px;
  top:-10px;
  height:140px;
  pointer-events:none;
  background:
    radial-gradient(520px 120px at 18% 40%, rgba(120,170,255,.14), transparent 72%),
    radial-gradient(520px 120px at 70% 40%, rgba(190,120,255,.10), transparent 74%);
  filter: blur(12px);
  opacity:.55;
}

/* РљР°СЂС‚РѕС‡РєРё: С‡СѓС‚СЊ СЃРІРµС‚Р»РµРµ вЂњРїРѕРІРµСЂС…РЅРѕСЃС‚СЊвЂќ, С‡РёС‚Р°РµРјРµРµ РіСЂР°РЅРёС†С‹ */
.dcard, .wcard{
  border-color: var(--xtir-line);
}
.dcard__body, .wcard{
  color: var(--xtir-text);
}

/* РЎС‚Р°С‚Р±Р°СЂ: РєРѕРЅС‚СЂР°СЃС‚РЅРµРµ С‡РёСЃР»Р° Рё РїРѕРґРїРёСЃРё */
.hero__stats{
  border-color: var(--xtir-line);
}
.hero__stats .hstat__n{
  color: rgba(140,190,255,.95);
  text-shadow: 0 0 18px rgba(120,170,255,.22);
}
.hero__stats .hstat__l{
  color: rgba(255,255,255,.62);
}

/* РљРЅРѕРїРєРё: Р°РєРєСѓСЂР°С‚РЅС‹Р№ РѕР±СЉС‘Рј */
.btn-primary, .btn-ghost{
  box-shadow: 0 10px 28px rgba(0,0,0,.30);
}
/* XTIR_HEADER_VISIBLE */
/* Р–С‘СЃС‚РєРѕ РІРѕР·РІСЂР°С‰Р°РµРј С€Р°РїРєСѓ РїРѕРІРµСЂС… hero/С„РѕРЅР° */
header, .header, .site-header, .topbar, [data-header], [role="banner"]{
  position: sticky !important;
  top: 0 !important;
  z-index: 9999 !important;
}

/* Р’РёРґРёРјРѕСЃС‚СЊ/РєРѕРЅС‚СЂР°СЃС‚ */
header, .header, .site-header, .topbar, [data-header], [role="banner"]{
  background: rgba(6,10,14,.72) !important;
  backdrop-filter: blur(10px) saturate(1.05);
  -webkit-backdrop-filter: blur(10px) saturate(1.05);
  border-bottom: 1px solid rgba(255,255,255,.10) !important;
}

/* РўРµРєСЃС‚/СЃСЃС‹Р»РєРё */
header a, .header a, .site-header a, .topbar a, [role="banner"] a{
  color: rgba(255,255,255,.86) !important;
}
header a:hover, .header a:hover, .site-header a:hover, .topbar a:hover, [role="banner"] a:hover{
  color: rgba(170,210,255,.98) !important;
}

/* РќР° РІСЃСЏРєРёР№ СЃР»СѓС‡Р°Р№: РµСЃР»Рё С€Р°РїРєСѓ вЂњСЃСЉРµРґР°РµС‚вЂќ СЂРѕРґРёС‚РµР»СЊ */
body, main, .page, .layout{
  overflow-x: hidden;
}
/* XTIR_TRICOLOR_TINT */
/* Р—Р°С‚РµРјРЅСЏРµРј/РїСЂРёРіР»СѓС€Р°РµРј С‚СЂРёРєРѕР»РѕСЂ-РїРѕР»РѕСЃРєСѓ РїРѕРґ С€Р°РїРєРѕР№ */
:root{
  --xtir-tricolor-dim: .28;     /* 0..1: РјРµРЅСЊС€Рµ = С‚РµРјРЅРµРµ */
  --xtir-tricolor-alpha: .15;   /* РїСЂРѕР·СЂР°С‡РЅРѕСЃС‚СЊ РїРѕР»РѕСЃРєРё */
}

/* РџРѕРїС‹С‚РєР° РїРѕР№РјР°С‚СЊ СЂР°Р·РЅС‹Рµ РІР°СЂРёР°РЅС‚С‹ РєР»Р°СЃСЃРѕРІ/Р°С‚СЂРёР±СѓС‚РѕРІ */
.tricolor, .tricolor-bar, .xtir-tricolor, .topbar__tricolor, .header__tricolor,
[data-tricolor], [data-xtir-tricolor]{
  opacity: var(--xtir-tricolor-alpha) !important;
  filter: brightness(var(--xtir-tricolor-dim)) saturate(.95) !important;
}

/* Р•СЃР»Рё С‚СЂРёРєРѕР»РѕСЂ РЅР°СЂРёСЃРѕРІР°РЅ РїСЃРµРІРґРѕСЌР»РµРјРµРЅС‚РѕРј */
.tricolor::before, .tricolor::after,
.tricolor-bar::before, .tricolor-bar::after,
.xtir-tricolor::before, .xtir-tricolor::after,
.topbar__tricolor::before, .topbar__tricolor::after,
.header__tricolor::before, .header__tricolor::after,
[data-tricolor]::before, [data-tricolor]::after,
[data-xtir-tricolor]::before, [data-xtir-tricolor]::after{
  opacity: var(--xtir-tricolor-alpha) !important;
  filter: brightness(var(--xtir-tricolor-dim)) saturate(.95) !important;
}

/* Р›С‘РіРєР°СЏ вЂњРґС‹РјРєР°вЂќ РїРѕРІРµСЂС…, С‡С‚РѕР±С‹ РІС‹РіР»СЏРґРµР»Рѕ С‚РµС…РЅРёС‡РЅРѕ Рё РЅРµ РєСЂРёС‡Р°Р»Рѕ */
.tricolor, .tricolor-bar, .xtir-tricolor, .topbar__tricolor, .header__tricolor,
[data-tricolor], [data-xtir-tricolor]{
  position: relative;
}
.tricolor::after, .tricolor-bar::after, .xtir-tricolor::after,
.topbar__tricolor::after, .header__tricolor::after,
[data-tricolor]::after, [data-xtir-tricolor]::after{
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  background: linear-gradient(to bottom, rgba(0,0,0,.72), rgba(0,0,0,.40));
}

/* XTIR_TRICOLOR_DIM_OVERRIDE */
:root{
  --xtir-tricolor-opacity: .33;      /* ~РІ 3 СЂР°Р·Р° С‚РёС€Рµ */
  --xtir-tricolor-brightness: .72;   /* С‡СѓС‚СЊ С‚РµРјРЅРµРµ */
  --xtir-tricolor-saturate: .80;     /* РјРµРЅСЊС€Рµ вЂњРєРёСЃР»РѕС‚С‹вЂќ */
}

/* Р›РѕРІРёРј СЃР°РјС‹Рµ С‡Р°СЃС‚С‹Рµ РІР°СЂРёР°РЅС‚С‹ РєР»Р°СЃСЃРѕРІ/Р±Р»РѕРєРѕРІ */
.tricolor,
.tricolor-bar,
.header-tricolor,
.xtir-tricolor,
.top-tricolor,
.xtir-topline,
[data-tricolor],
[aria-label*="С‚СЂРёРєРѕР»РѕСЂ" i],
[aria-label*="tricolor" i]{
  opacity: var(--xtir-tricolor-opacity) !important;
  filter: brightness(var(--xtir-tricolor-brightness)) saturate(var(--xtir-tricolor-saturate)) !important;
}

/* Р•СЃР»Рё С‚СЂРёРєРѕР»РѕСЂ РЅР°СЂРёСЃРѕРІР°РЅ РїСЃРµРІРґРѕСЌР»РµРјРµРЅС‚РѕРј */
.tricolor::before, .tricolor::after,
.tricolor-bar::before, .tricolor-bar::after,
.header-tricolor::before, .header-tricolor::after,
.xtir-tricolor::before, .xtir-tricolor::after,
.top-tricolor::before, .top-tricolor::after,
.xtir-topline::before, .xtir-topline::after,
[data-tricolor]::before, [data-tricolor]::after{
  opacity: var(--xtir-tricolor-opacity) !important;
  filter: brightness(var(--xtir-tricolor-brightness)) saturate(var(--xtir-tricolor-saturate)) !important;
}
/* XTIR_TRICOLOR_DIM_OVERRIDE END */
</style>

<style>
  /* Все переменные локальные */
  .hp-hero, .hp-sec, .hp-wrap, .hp-cards, .hp-why, .hp-cta {
    --ac: #00b3ff; --acw: #ff6b35;
    --bg: #0f141a; --bg2: #0b0e13; --sf: #141a22;
    --bd: rgba(255,255,255,0.07);
    --tx: rgba(255,255,255,0.88); --mu: rgba(255,255,255,0.45);
  }

  /* Триколор элементы */
  .tc1 { flex: 1; background: #ffffff !important; }
  .tc2 { flex: 1; background: #0039a6 !important; }
  .tc3 { flex: 1; background: #d52b1e !important; }

  /* ── ГЕРОЙ ── */
  .hp-hero {
    position: relative; min-height: 100vh;
    display: flex; flex-direction: column;
    overflow: hidden; background: #060a0e;
  }
  .hp-bg { position: absolute; inset: 0; }
  .hp-glow-l {
    position: absolute; width: 800px; height: 800px;
    top: -200px; left: -300px; border-radius: 50%;
    background: radial-gradient(circle, rgba(0,179,255,0.18), transparent 70%);
    filter: blur(100px);
  }
  .hp-glow-r {
    position: absolute; width: 600px; height: 600px;
    top: 0; right: -200px; border-radius: 50%;
    background: radial-gradient(circle, rgba(0,60,180,0.15), transparent 70%);
    filter: blur(80px);
  }
  .hp-grid {
    position: absolute; inset: 0;
    background-image:
      linear-gradient(rgba(0,179,255,0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,179,255,0.05) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 70% 70% at 50% 50%, black, transparent);
  }
  .hp-scan {
    position: absolute; inset: 0;
    background: linear-gradient(180deg, transparent 35%, rgba(0,179,255,0.04) 50%, transparent 65%);
    animation: hpscan 14s ease-in-out infinite;
  }
  @keyframes hpscan { 0%,100%{transform:translateY(-50%)} 50%{transform:translateY(50%)} }

  .hp-tricolor {
    position: absolute; top: 0; left: 0; right: 0;
    height: 4px; display: flex; z-index: 10;
  }

  /* Угловые маркеры */
  .hp-c { position: absolute; width: 26px; height: 26px; border-color: rgba(0,179,255,0.6); border-style: solid; z-index: 5; }
  .hp-c-tl { top: 10px; left: 10px; border-width: 2px 0 0 2px; border-radius: 3px 0 0 0; }
  .hp-c-tr { top: 10px; right: 10px; border-width: 2px 2px 0 0; border-radius: 0 3px 0 0; }
  .hp-c-bl { bottom: 58px; left: 10px; border-width: 0 0 2px 2px; border-radius: 0 0 0 3px; }
  .hp-c-br { bottom: 58px; right: 10px; border-width: 0 2px 2px 0; border-radius: 0 0 3px 0; }

  /* ── ПУЛЯ ── медленная, реалистичная */
  .hp-bullet-wrap {
    position: absolute; inset: 0; overflow: hidden;
    pointer-events: none; z-index: 6;
  }
  .hp-bullet {
    position: absolute;
    width: 200px; height: 2px;
    background: linear-gradient(to right,
      transparent 0%,
      rgba(255,180,30,0.3) 20%,
      rgba(255,220,80,0.85) 60%,
      rgba(255,255,200,1) 85%,
      rgba(255,255,255,0.5) 100%
    );
    box-shadow: 0 0 6px rgba(255,200,50,0.7), 0 0 16px rgba(255,130,0,0.4);
    border-radius: 2px;
    top: 35%; left: -220px;
    animation: hpbullet 7s ease-in 2s infinite;
  }
  .hp-bullet--2 {
    top: 55%; left: -220px;
    animation: hpbullet2 9s ease-in 6s infinite;
    opacity: 0.6;
    width: 150px;
  }
  @keyframes hpbullet {
    0%   { left: -220px; top: 35%; opacity: 0; }
    5%   { opacity: 1; }
    20%  { left: 25%; top: 42%; }
    50%  { left: 65%; top: 52%; }
    75%  { left: 95%; top: 59%; opacity: 0.4; }
    80%  { left: 105%; opacity: 0; }
    100% { left: 105%; opacity: 0; }
  }
  @keyframes hpbullet2 {
    0%   { left: -180px; top: 55%; opacity: 0; }
    5%   { opacity: 0.6; }
    30%  { left: 35%; top: 48%; }
    60%  { left: 70%; top: 42%; opacity: 0.3; }
    70%  { left: 105%; opacity: 0; }
    100% { left: 105%; opacity: 0; }
  }

  .hp-body {
    flex: 1; display: flex; align-items: center; justify-content: center;
    position: relative; z-index: 3; padding: 120px 24px 60px;
  }
  .hp-center { text-align: center; max-width: 820px; }

  .hp-badge {
    display: inline-flex; align-items: center; gap: 8px;
    font-family: 'Courier New', monospace;
    font-size: .68rem; letter-spacing: .18em; text-transform: uppercase;
    color: #00b3ff; margin-bottom: 22px;
    background: rgba(0,179,255,0.08); border: 1px solid rgba(0,179,255,0.2);
    padding: 6px 16px; border-radius: 20px;
  }
  .hp-bdot {
    width: 6px; height: 6px; border-radius: 50%;
    background: #00b3ff; box-shadow: 0 0 8px #00b3ff;
    animation: hppulse 2s ease-in-out infinite; flex-shrink: 0;
  }
  @keyframes hppulse { 0%,100%{opacity:1} 50%{opacity:.2} }

  .hp-logo-box { margin-bottom: 24px; }
  .hp-logo { height: 72px; width: auto; filter: drop-shadow(0 0 28px rgba(0,179,255,0.55)) drop-shadow(0 0 56px rgba(0,100,200,0.3)); }

  .hp-slogan { display: flex; align-items: center; gap: 14px; justify-content: center; margin-bottom: 16px; }
  .hp-sbar { display: flex; flex-direction: column; width: 4px; height: 52px; border-radius: 2px; overflow: hidden; flex-shrink: 0; }
  .hp-stext {
    font-size: clamp(19px, 2.6vw, 32px);
    font-weight: 700; color: #ffffff !important;
    text-align: left; line-height: 1.25;
    text-shadow: 0 2px 16px rgba(0,0,0,0.9);
    /* Перебиваем Tailwind h-классы */
    font-family: inherit !important;
    letter-spacing: -0.01em !important;
  }

  .hp-desc {
    font-size: .98rem; color: rgba(255,255,255,0.5) !important;
    line-height: 1.7; max-width: 500px; margin: 0 auto 30px;
    text-shadow: 0 1px 8px rgba(0,0,0,0.95);
    font-family: inherit !important; font-weight: normal !important;
  }
  .hp-acts { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; }

  .hp-btn1, .hp-btn2 {
    display: inline-flex; align-items: center;
    padding: 13px 30px; border-radius: 8px;
    font-size: .92rem; font-weight: 700;
    text-decoration: none !important; transition: all .2s;
    font-family: inherit !important;
  }
  .hp-btn1 { background: #00b3ff; color: #000 !important; }
  .hp-btn1:hover { background: #00c8ff; transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,179,255,0.35); }
  .hp-btn2 { color: rgba(255,255,255,0.88) !important; border: 1px solid rgba(255,255,255,0.2); background: rgba(255,255,255,0.07); backdrop-filter: blur(8px); }
  .hp-btn2:hover { background: rgba(255,255,255,0.13); }

  /* Стат бар */
  .hp-stats {
    position: relative; z-index: 3;
    border-top: 1px solid rgba(255,255,255,0.08);
    background: rgba(0,0,0,0.6); backdrop-filter: blur(16px);
    display: flex; align-items: center; justify-content: center;
    gap: 48px; padding: 20px 24px; flex-wrap: wrap;
  }
  .hp-stat { text-align: center; }
  .hp-sn { font-size: 1.65rem; font-weight: 800; color: #00b3ff !important; line-height: 1; font-family: inherit !important; }
  .hp-sl { font-family: 'Courier New', monospace !important; font-size: .6rem; text-transform: uppercase; letter-spacing: .1em; color: rgba(255,255,255,0.45); margin-top: 3px; }
  .hp-sdiv { width: 1px; height: 34px; background: rgba(255,255,255,0.07); }
  @media(max-width:600px){ .hp-sdiv{display:none} }

  /* ── ОБЩИЕ ── */
  .hp-sec { padding: 90px 0; }
  .hp-dark { background: #0f141a; }
  .hp-darker { background: #0b0e13; border-top: 1px solid rgba(255,255,255,0.07); border-bottom: 1px solid rgba(255,255,255,0.07); }
  .hp-wrap { max-width: 1200px; margin: 0 auto; padding: 0 24px; }
  .hp-hd { margin-bottom: 44px; }
  .hp-kicker { font-family: 'Courier New', monospace !important; font-size: .68rem; letter-spacing: .2em; text-transform: uppercase; color: #00b3ff; opacity: .75; margin-bottom: 10px; }

  /* Перебиваем Tailwind заголовки */
  .hp-title {
    font-size: clamp(24px, 3.2vw, 44px) !important;
    font-weight: 800 !important; line-height: 1.08 !important;
    letter-spacing: -0.02em !important;
    color: #ffffff !important; margin: 0 0 14px !important;
    font-family: inherit !important;
    text-transform: none !important;
  }
  .hp-sub {
    font-size: .98rem !important; line-height: 1.7 !important;
    color: rgba(255,255,255,0.45) !important; margin: 0 !important;
    font-family: inherit !important; font-weight: normal !important;
    text-transform: none !important;
  }
  .hp-lnk { display: inline-block; margin-top: 24px; color: #00b3ff !important; text-decoration: none !important; font-weight: 600; transition: opacity .2s; }
  .hp-lnk:hover { opacity: .7; }

  /* Карточки направлений */
  .hp-cards { display: grid; grid-template-columns: repeat(3,1fr); gap: 16px; }
  @media(max-width:900px){ .hp-cards{ grid-template-columns:1fr; } }

  .hp-card {
    background: #141a22; border: 1px solid rgba(255,255,255,0.07);
    border-radius: 16px; overflow: hidden;
    text-decoration: none !important; color: rgba(255,255,255,0.88) !important;
    display: flex; flex-direction: column; transition: all .25s;
  }
  .hp-card:hover { border-color: rgba(0,179,255,0.35); transform: translateY(-6px); box-shadow: 0 20px 40px rgba(0,0,0,0.5); }
  .hp-card-warm:hover { border-color: rgba(255,107,53,0.35); }

  .hp-cimg { position: relative; aspect-ratio: 16/10; overflow: hidden; background: #0a0d12; }
  .hp-cimg img { width: 100%; height: 100%; object-fit: cover; transition: transform .4s ease; opacity: .85; }
  .hp-card:hover .hp-cimg img { transform: scale(1.06); opacity: 1; }
  .hp-cimgo { position: absolute; inset: 0; background: linear-gradient(to bottom, transparent 40%, rgba(10,13,18,0.85) 100%); }
  .hp-cnum {
    position: absolute; top: 12px; left: 12px;
    font-family: 'Courier New', monospace; font-size: .62rem; color: #00b3ff;
    background: rgba(0,0,0,0.65); border: 1px solid rgba(0,179,255,0.3);
    padding: 3px 9px; border-radius: 2px; letter-spacing: .1em;
  }
  .hp-card-warm .hp-cnum { color: #ff6b35; border-color: rgba(255,107,53,0.3); }

  .hp-cbody { padding: 22px; display: flex; flex-direction: column; gap: 9px; flex: 1; }
  .hp-ct { font-size: 1.08rem !important; font-weight: 700 !important; color: #fff !important; font-family: inherit !important; text-transform: none !important; }
  .hp-cd { font-size: .86rem !important; color: rgba(255,255,255,0.45) !important; line-height: 1.6; flex: 1; font-family: inherit !important; font-weight: normal !important; }
  .hp-ctags { display: flex; gap: 6px; flex-wrap: wrap; }
  .hp-ctags span { font-family: 'Courier New', monospace; font-size: .58rem; text-transform: uppercase; letter-spacing: .08em; color: rgba(255,255,255,0.4); border: 1px solid rgba(255,255,255,0.07); padding: 2px 8px; border-radius: 2px; }
  .hp-cmore { font-size: .84rem !important; font-weight: 600 !important; color: #00b3ff !important; margin-top: 4px; font-family: inherit !important; }
  .hp-cmore-warm { color: #ff6b35 !important; }

  /* Почему XTIR */
  .hp-why { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: start; }
  @media(max-width:900px){ .hp-why{ grid-template-columns:1fr; gap:40px; } }
  .hp-wr { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  .hp-wc { background: #141a22; border: 1px solid rgba(255,255,255,0.07); border-radius: 12px; padding: 22px; transition: border-color .2s; }
  .hp-wc:hover { border-color: rgba(0,179,255,0.2); }
  .hp-wi { font-size: 1.35rem; margin-bottom: 10px; }
  .hp-wt { font-size: .9rem !important; font-weight: 700 !important; color: #fff !important; margin-bottom: 7px; font-family: inherit !important; text-transform: none !important; }
  .hp-wd { font-size: .83rem !important; color: rgba(255,255,255,0.45) !important; line-height: 1.6; font-family: inherit !important; font-weight: normal !important; }

  /* CTA */
  .hp-ctabg { position: absolute; inset: 0; background: radial-gradient(ellipse 900px 400px at 50% 100%, rgba(0,179,255,0.07), transparent); pointer-events: none; }
  .hp-cta { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: center; position: relative; }
  @media(max-width:768px){ .hp-cta{ grid-template-columns:1fr; gap:40px; } }
  .hp-ctr { display: flex; flex-direction: column; gap: 20px; }
  .hp-cnt { display: flex; flex-direction: column; gap: 3px; text-decoration: none !important; transition: opacity .2s; }
  .hp-cnt:hover { opacity: .7; }
  .hp-cl { font-family: 'Courier New', monospace !important; font-size: .62rem; text-transform: uppercase; letter-spacing: .12em; color: rgba(255,255,255,0.4); }
  .hp-cv { font-size: 1.08rem !important; font-weight: 700 !important; color: #fff !important; font-family: inherit !important; }

/* XTIR_TRICOLOR_DIM_OVERRIDE */
:root{
  --xtir-tricolor-opacity: .33;      /* ~РІ 3 СЂР°Р·Р° С‚РёС€Рµ */
  --xtir-tricolor-brightness: .72;   /* С‡СѓС‚СЊ С‚РµРјРЅРµРµ */
  --xtir-tricolor-saturate: .80;     /* РјРµРЅСЊС€Рµ вЂњРєРёСЃР»РѕС‚С‹вЂќ */
}

/* Р›РѕРІРёРј СЃР°РјС‹Рµ С‡Р°СЃС‚С‹Рµ РІР°СЂРёР°РЅС‚С‹ РєР»Р°СЃСЃРѕРІ/Р±Р»РѕРєРѕРІ */
.tricolor,
.tricolor-bar,
.header-tricolor,
.xtir-tricolor,
.top-tricolor,
.xtir-topline,
[data-tricolor],
[aria-label*="С‚СЂРёРєРѕР»РѕСЂ" i],
[aria-label*="tricolor" i]{
  opacity: var(--xtir-tricolor-opacity) !important;
  filter: brightness(var(--xtir-tricolor-brightness)) saturate(var(--xtir-tricolor-saturate)) !important;
}

/* Р•СЃР»Рё С‚СЂРёРєРѕР»РѕСЂ РЅР°СЂРёСЃРѕРІР°РЅ РїСЃРµРІРґРѕСЌР»РµРјРµРЅС‚РѕРј */
.tricolor::before, .tricolor::after,
.tricolor-bar::before, .tricolor-bar::after,
.header-tricolor::before, .header-tricolor::after,
.xtir-tricolor::before, .xtir-tricolor::after,
.top-tricolor::before, .top-tricolor::after,
.xtir-topline::before, .xtir-topline::after,
[data-tricolor]::before, [data-tricolor]::after{
  opacity: var(--xtir-tricolor-opacity) !important;
  filter: brightness(var(--xtir-tricolor-brightness)) saturate(var(--xtir-tricolor-saturate)) !important;
}
/* XTIR_TRICOLOR_DIM_OVERRIDE END */
</style>

<!-- XTIR_NO_BULLETS_MAX_JS -->
<script is:inline>
(() => {
  const killRe = /(bullet|tracer|streak|shot|beam|laser|ray|sweep|scan|line)/i;
  const root = document.querySelector('.hero') || document;
  const sweep = () => {
    root.querySelectorAll('[class]').forEach(el => {
      const cls = (el.className && el.className.toString()) || '';
      if (killRe.test(cls)) el.remove();
    });
    root.querySelectorAll('[style]').forEach(el => {
      const st = el.getAttribute('style') || '';
      if (/animation/i.test(st) && /(bullet|tracer|streak|shot|beam|laser|ray|sweep|scan|line)/i.test(st)) el.remove();
    });
  };
  sweep();
  // РЅР° СЃР»СѓС‡Р°Р№, РµСЃР»Рё СЌС„С„РµРєС‚С‹ СЃРѕР·РґР°СЋС‚СЃСЏ РїРѕСЃР»Рµ Р·Р°РіСЂСѓР·РєРё
  const mo = new MutationObserver(() => sweep());
  mo.observe(root, { subtree:true, childList:true });
  setTimeout(() => mo.disconnect(), 8000);
})();
</script>

